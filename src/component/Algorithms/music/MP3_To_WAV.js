import ffmpeg from "ffmpeg";

export const mp3_to_wav = file => {
  try {
    // var blob = new File([file], "test.mp3", {
    //   type: " audio/wav"
    // });
    // console.log(blob);
    ffmpeg();
  } catch (error) {}
};
